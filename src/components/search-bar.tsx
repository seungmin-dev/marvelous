import styled from "@emotion/styled";

const Wrapper = styled.div`
  width: 100%;
  height: 100vh;
  background-color: teal;
`;

export default function SearchBar() {
  return <Wrapper>search bar</Wrapper>;
}
